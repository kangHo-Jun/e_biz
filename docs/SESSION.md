# SESSION LOG (2026-04-01)

## 작업 요약
- 자동치수 로직 수정 전, 안정적인 복구 지점 확보를 위한 Git 커밋 수행.
- 커밋 메시지: `v0401_자동치수로직 수정전`

## 상세 내역
1. **버전 관리**: `add_colormap.py` 등 현재 변경 사항을 스테이징(Staging) 및 커밋(Commit) 완료.
2. **백업**: 주요 로직 수정 전 베이스라인 브랜치 상태 저장.

### 2026-04-01 (2nd Update)
- 문짝치수 자동 생성 로직 (`생성_문짝치수`) 구현 및 메뉴 연동.
- 문제 진단을 위한 `진단_문짝치수` 함수 추가.
- `CONFIG.COLS.AZ` (52번 열) 설정 추가.
- 로직: 3방 (A-68 * B-65), 4방 (A-68 * B-(30+AZ))

### 2026-04-01 (3rd Update: 배포 완료)
- **CLASP 배포**: `clasp push -f`를 통해 `door_v4.gs` 수정 사항을 Google Apps Script 서버에 최종 반영 완료.
- **배포 확인**: Pushed 2 files (dist/appsscript.json, door_v4.gs) 확인됨.
### 2026-04-01 (6th Update: 연산 로직 재수정)
- **생성_문짝치수() 교정**: B값(높이) 계산 시 3방/4방 조건이 상호 교체되어 있던 부분을 바로잡음.
    - 4방: 무조건 `B - 65`
    - 3방 & AZ 미입력: `B - 65`
    - 3방 & AZ 입력: `B - (30 + AZ)`
- **검증**: 제시된 3가지 케이스(720*2080 기준) 결과값이 기대값(2040, 2015, 2015)과 일치함을 확인.
- **배포**: `clasp push -f`를 통해 서버 반영 완료.

---

# SESSION LOG (2026-03-20)

## 작업 요약
- `door_v4.gs` 내 `색상_전처리` 함수의 정규식 패턴을 개선하여 괄호가 포함된 브랜드 색상 코드를 완벽하게 추출하도록 수정함.
- 10가지 테스트 케이스 검증 결과, 기대값과 100% 일치함을 확인하고 서버에 최종 반영 완료.

## 상세 내역
1. **로직 수정**: `영림147(PW951-1)`과 같은 입력을 `영림147PW951-1`로 변환하도록 패턴 추가.
2. **검증**: 10개 색상 케이스(PS/PX/영림숫자/순한글 등) 통합 테스트 통과.
3. **배포**: `clasp push -f`를 통해 Google Apps Script 서버 반영.
4. **문서**: `docs/품목생성로직.md`에 업데이트 이력 기록.

## 다음 작업 (Next Step)
- 생성된 품목코드를 기반으로 ERP 실제 등록 테스트 진행 지원.
